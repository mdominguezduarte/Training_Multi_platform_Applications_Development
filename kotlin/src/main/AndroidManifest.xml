<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pac_desarrollo_08">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PAC_DESARROLLO_08">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GestionarDatos" />
        <activity android:name=".Servicios" />
        <activity android:name=".CrearTabla" />
        <activity android:name=".InsertarDatos" />
        <activity android:name=".MostrarDatos" />
        <activity android:name=".Musica" />

        <service
            android:name=".MiIntentService"
            android:exported="false" />
    </application>

</manifest>